//Below is a list of the Sass rules that could not be converted to Stylus
// @while: line 60 in your Sass file
// @while: line 72 in your Sass file
// @while: line 89 in your Sass file
// @while: line 101 in your Sass file
// @while: line 119 in your Sass file
// @while: line 131 in your Sass file


.container
  margin: 0 auto
  max-width: 1280px
  width: 90%
@media $medium-and-up
  .container
    width: 85%
@media $large-and-up
  .container
    width: 70%
.container .row
  margin-left: (-1 * $gutter-width / 2)
  margin-right: (-1 * $gutter-width / 2)
.section
  padding-top: 1rem
  padding-bottom: 1rem
  &.no-pad
    padding: 0
  &.no-pad-bot
    padding-bottom: 0
  &.no-pad-top
    padding-top: 0
.row
  margin-left: auto
  margin-right: auto
  margin-bottom: 20px
  // Clear floating children
  &:after
    content: ""
    display: table
    clear: both
  .col
    float: left
    box-sizing: border-box
    padding: 0 ($gutter-width / 2)
    &[class*="push-"],
    &[class*="pull-"]
      position: relative

    for $i in 1..$num-cols
      $perc = 100 / ($num-cols / $i) + "%"
      &.s$i
        width: $perc
        margin-left: auto
        left: auto
        right: auto

    for $i in 1..$num-cols
      $perc= 100 / ($num-cols / $i) + "%"
      &.offset-s$i
        margin-left: $perc
      &.pull-s$i
        right: $perc
      &.push-s$i
        left: $perc
    @media $medium-and-up
      for $i in 1..$num-cols
        $perc= 100 / ($num-cols / $i) + "%"
        &.m$i
          width: $perc
          margin-left: auto
          left: auto
          right: auto

      for $i in 1..$num-cols
        $perc= 100 / ($num-cols / $i) + "%"
        &.offset-m$i
          margin-left: $perc
        &.pull-m$i
          right: $perc
        &.push-m$i
          left: $perc

    @media $large-and-up
      for $i in 1..$num-cols
        $perc= 100 / ($num-cols / $i) + "%"
        &.l$i
          width: $perc
          margin-left: auto
          left: auto
          right: auto
      $i = 1

      for $i in 1..$num-cols
        $perc= 100 / ($num-cols / $i) + "%"
        &.offset-l$i
          margin-left: $perc
        &.pull-l$i
          right: $perc
        &.push-l$i
          left: $perc